spring.application.name=HouseholdService
server.address=0.0.0.0
server.port=${SERVER_PORT:8096}

userservice.service-token=${USER_SERVICE_TOKEN:local-internal-service-token}
userservice.base-url=${USER_SERVICE_BASE_URL:http://localhost:8092}
jwt.secret=${JWT_SECRET:ZGV2LWZvb2QtYW5hbHl6aW5nLWp3dC1zZWNyZXQtbG9jYWwtc3RhY2stMTIzNDU2Nzg5MA==}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/households}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.jpa.database=postgresql
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# Flyway owns schema changes. baseline-on-migrate preserves an existing schema on first run.
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-disabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true
eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://localhost:8761/eureka/}

eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${HOSTNAME:${random.value}}

logging.file.name=logs/${spring.application.name}.log
logging.logback.rollingpolicy.max-file-size=5MB
logging.logback.rollingpolicy.max-history=7

management.tracing.sampling.probability=1.0
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*
